import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#002B5B",
    },
    secondary: {
      main: "#21E1E1",
    },
    background: {
      default: "#F5F5F5",
    },
  },
});

export default theme;