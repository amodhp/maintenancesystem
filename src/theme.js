import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#FFA500",
    },
    secondary: {
      main: "#21E1E1",
    },
    background: {
      default: "#e8e8e8",
    },
  },
});

export default theme;